compile:
  # Compile tests for Arduino Uno platform
  platforms:
    - uno

unittest:
  # Unit test platforms and dependencies
  platforms:
    - uno
  libraries:
    - "OneWire"    # Required dependency for DallasTemperature

compile_options:
  libraries:
    - "OneWire"    # Required dependency for DallasTemperature

verify_options:
  libraries:
    - "OneWire"    # Required dependency for DallasTemperature